<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_songs_path(artist) %></li> #Creates a link to the index of all songs made by this artist.
  <% end %>
</ul>
